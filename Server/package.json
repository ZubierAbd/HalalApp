{
  "name": "halalapp",
  "version": "1.0.0",
  "description": "this is the server for the isithalal backend. Ideally should have only send back json response for the user telling them what restaurants are halal or not.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.7.1",
    "nodemon": "^1.19.2"
  }
}
